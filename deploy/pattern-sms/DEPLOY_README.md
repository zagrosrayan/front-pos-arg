# Pattern SMS frontend — changed files only

Unzip into the Next.js project root (`Hotel Arg Pos`).

## Files in this zip

- `app/components/DataTable/NextPurchaseDiscountTable/NextPurchaseDiscountTable.tsx`
- `app/components/Checkout/Checkout.tsx`
- `app/constant/label.ts`

## Notes

- UI explains automatic immediate discount SMS + reminder (no SMS template settings).
- Removed obsolete `send_sms` payload flags and unused SMS labels.
